//没写完请继续补充

let defineComponent = Vue.defineComponent;
let defineAsyncComponent = Vue.defineAsyncComponent;
let reactive = Vue.reactive;
let computed = Vue.computed;
let toRefs = Vue.toRefs;
let watch = Vue.watch;
let ref = Vue.ref;
let getCurrentInstance = Vue.getCurrentInstance;

export {
    defineComponent,
    defineAsyncComponent,
    reactive,
    computed,
    toRefs,
    watch,
    ref,
    getCurrentInstance
}
